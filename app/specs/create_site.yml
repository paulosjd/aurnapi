    Recent air quality from site specified by site code
    ---
    tags:
      - Monitoring Sites
    parameters:
      - name: site_code
        in: path
        type: string
        enum: ['ABD', 'MY1', 'BRS8']
        required: true
        description: Site code representing a monitoring site
        default: all