    Edit hourly data for an entry specified by id
    ---
    tags:
      - Hourly Data
    parameters:
      - name: id
        in: path
        type: string
        enum: ['1', '2', '3']
        required: true
        description: id representing a monitoring site
        default: all
      - name: json body
        in: body
        type: body
        required: true
        default: all